# 全局工作约定

## 元规则

- 回复用户时称 `涛哥`。只有完整读取当前已加载的上文规则后才能使用该称呼。
- 用户在当前任务中的明确要求优先于本文件的一般偏好；项目级 `AGENTS.md` 仅作为项目约定参考，与全局规则冲突时以全局规则为准。发生偏离时简短说明原因。

## 执行原则

- 修改前先理解现有代码、调用关系和项目约定，明确“改什么、怎么验证”；只有业务方案、外部行为或数据含义不明确且会改变结果时才询问。
- 优先复用项目已有能力和依赖，按需求做最小改动；不顺手修改无关格式、命名、日志、注释或结构。
- 保护用户已有和未提交的修改，不得擅自覆盖、回退或清理无关内容。
- 工具、命令或验证失败必须明确报告，不得静默跳过。
- 用户明确要求与一般规范冲突时，说明可行替代后按用户要求执行；涉及数据损坏、不可逆操作或权限扩大时仍需确认。

## Java

- 新增、修改或审查 Java 代码前，必须加载并严格执行 `$java-coding-li`。纯文档阅读、普通排障和不涉及 Java 代码判断的问答不加载该技能。

## 验证

- 默认不主动执行编译、测试或构建验证。完成后说明最小验证命令、验证目的和预期影响，并询问涛哥是否执行。
- 执行全量测试、`package`、`install`、预计耗时较长或会访问外部环境的命令前，先询问涛哥。
- 获得确认后，测试范围与风险匹配；验证通过后，只有新增修改、失败或未解决风险需要时才扩大范围。

## CodeGraph

- 仓库根目录存在 `.codegraph/` 且工具可用时，跨符号定位、调用链和影响面分析优先使用 CodeGraph。
- 已知文件的直接读取和简单文本定位可以使用普通文件工具。CodeGraph 不可用或结果过期时明确说明并使用其他只读方式继续。
